spring.application.name=seguridadcalidad

# URL base del backend que emite JWT
backend.url=${BACKEND_URL:http://localhost:8081}

# Cookie JWT: deshabilitar "Secure" solo en desarrollo local sin HTTPS
# En producción debe ser siempre true
app.cookie.secure=${COOKIE_SECURE:true}

